#include "FEHLCD.h"

/**
 * Entry point to the application.
 * 
 * @returns status code of program exit
 */
int main() {
    LCD.Clear();
    LCD.WriteLine("Hello World!");
    while (1) {
        // Run forever
    }
    return 0;
}